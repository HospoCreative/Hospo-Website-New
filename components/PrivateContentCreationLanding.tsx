import Link from "next/link";
import { ArrowUpRight, Check } from "lucide-react";
import { contentCreationPackages } from "@/data/contentCreationLanding";
import { displayPrice } from "@/lib/commercial";
import { SectionHeading } from "./SectionHeading";

export function PrivateContentCreationLanding() {
  const enquiryHref = "/contact?package=Content%20Plus";

  return <article className="overflow-hidden rounded-[8px] border border-ink/10 bg-white shadow-soft">
    <section className="bg-ink px-5 py-14 text-white sm:px-8 sm:py-20"><div className="mx-auto max-w-7xl"><p className="section-eyebrow text-yellow">Content creation packages</p><h1 className="mt-5 max-w-5xl font-serif text-[clamp(3rem,6vw,5.5rem)] font-semibold leading-[.92]">Professional content for the places people discover and decide.</h1><p className="mt-7 max-w-3xl text-lg leading-8 text-white/72">Flexible photography and short-form video packages for restaurants, hotels, bars, cafés, accommodation and lifestyle businesses.</p><a href="#packages" className="button-primary mt-8">Explore packages <ArrowUpRight size={16} /></a></div></section>
    <section id="packages" className="px-5 py-[var(--hc-section-compact)] text-ink sm:px-8"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="Content creation" title="Choose the content coverage that fits the brief." body="Each package is designed to give your business a useful, platform-ready bank of visual content."/><div className="mt-12 grid gap-5 lg:grid-cols-3">{contentCreationPackages.map((item) => <article key={item.id} className={`flex flex-col rounded-[8px] border p-7 ${item.featured ? "border-ink bg-ink text-white lg:-translate-y-3 lg:py-9" : "border-ink/15 bg-white text-ink"}`}><p className={`section-eyebrow ${item.featured ? "text-yellow" : "text-ink/55"}`}>{item.badge || ""}</p><h2 className="mt-4 font-serif text-4xl leading-none">{item.name}</h2><p className={`mt-4 text-2xl font-bold ${item.featured ? "text-yellow" : "text-ink"}`}>{displayPrice(item)}</p><p className={`mt-5 min-h-[3rem] text-sm leading-6 ${item.featured ? "text-white/72" : "text-ink/67"}`}>{item.description}</p><ul className="mt-7 space-y-3">{item.included_items.map((included) => <li key={included} className={`flex gap-3 text-sm leading-6 ${item.featured ? "text-white/88" : "text-ink/80"}`}><Check size={17} className="mt-0.5 shrink-0 text-yellow"/>{included}</li>)}</ul><Link href={enquiryHref} className={`mt-8 inline-flex items-center gap-2 text-[0.66rem] font-black uppercase tracking-[0.14em] ${item.featured ? "text-yellow" : "text-ink"}`}>Plan Your Content Shoot <ArrowUpRight size={16}/></Link></article>)}</div><p className="mt-10 max-w-5xl border-l-2 border-yellow pl-5 text-sm leading-7 text-ink/70">Every shoot is tailored to the business. Content can focus on areas such as food and drinks, rooms and interiors, products, services, team, atmosphere, guest experience, events or specific campaigns depending on what the client wants to promote.</p></div></section>
    <section className="bg-ink px-5 py-12 text-white sm:px-8"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-6 md:flex-row md:items-center"><div><p className="section-eyebrow text-yellow">Next step</p><h2 className="mt-3 max-w-3xl font-serif text-4xl leading-tight">Plan your next content shoot around what matters most.</h2></div><Link href={enquiryHref} className="button-primary shrink-0">Plan Your Content Shoot <ArrowUpRight size={16}/></Link></div></section>
  </article>;
}
