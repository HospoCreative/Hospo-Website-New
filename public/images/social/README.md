# Social Images

Use this folder for fallback images that appear in the Socials section.

The site can show a live Instagram feed when `INSTAGRAM_ACCESS_TOKEN` is configured.

If the token is missing or Instagram is unavailable, the site uses images from `instagram`.

See `INSTAGRAM_LIVE_FEED.md` in the project root for setup instructions.
