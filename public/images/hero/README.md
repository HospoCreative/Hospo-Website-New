# Hero Images

Use the two folders inside this folder:

- `background` - controls the large dark hero background image.
- `collage` - controls the four image collage on the right side of the hero.

You do not need to edit `data/site.ts` for hero images anymore.
