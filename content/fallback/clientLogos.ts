import type { ClientLogo } from "@/types/clientLogo";

// Add only genuine supplied client logos here.
// The public logo section remains hidden until at least three published logos exist.
export const fallbackClientLogos: ClientLogo[] = [];
